<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amway">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_amway"
        android:supportsRtl="true"
        android:theme="@style/Theme.Amway">
        <activity android:name=".ViewWtoPlan"></activity>
        <activity android:name=".ViewStockCount" />
        <activity android:name=".CheckStock" />
        <activity android:name=".Setup" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>